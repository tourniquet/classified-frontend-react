const initialState = {
  username: '',
  password: '',
  confirmPassword: ''
}

export const registrationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
